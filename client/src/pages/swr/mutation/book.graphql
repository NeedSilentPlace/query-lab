query Book($bookId: String!) {
  book(id: $bookId) {
    id
    title
    author
    description
    thumbnail
  }
}

mutation UpdateBook($input: BookUpdateInput!) {
  updateBook(input: $input) {
    id
    title
    author
    description
    thumbnail
  }
}
