## Server
`apollo-server`

```bash
cd server
yarn install
yarn dev
```

## Client
`next-js` + `react-query` + `graphql-request` + `graphql-codegenerator`

```bash
cd client
yarn install
yarn codegen (server 켜야해요)
yarn dev
```
